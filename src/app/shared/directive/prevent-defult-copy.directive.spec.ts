import { PreventDefultCopyDirective } from './prevent-defult-copy.directive';

describe('PreventDefultCopyDirective', () => {
  it('should create an instance', () => {
    const directive = new PreventDefultCopyDirective();
    expect(directive).toBeTruthy();
  });
});
